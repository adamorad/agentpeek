// Package version holds the build identity for the airlock binary.
package version

const (
	// Number is the semantic version of the airlock binary.
	Number = "2.0.0-dev"
	// Name is the binary/product name used in CLI output.
	Name = "airlock"
)
